source "https://rubygems.org"

# The github-pages gem pins Jekyll and its plugins to the exact versions GitHub
# Pages runs, so a local preview and the deployed site cannot diverge.
gem "github-pages", group: :jekyll_plugins

# Ruby 3.4 dropped these from the standard library.
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"
