{
  "name": "azure-sql-dev-hub",
  "private": true,
  "description": "Build and check scripts for the Azure SQL Developer Hub site.",
  "type": "module",
  "scripts": {
    "test": "node scripts/check-house-rules.mjs",
    "build:agent-files": "node scripts/build-agent-files.mjs",
    "check:links": "node scripts/check-links.mjs"
  },
  "dependencies": {
    "js-yaml": "^4.3.2"
  }
}
